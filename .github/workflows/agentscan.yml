name: AgentScan

on:
  pull_request_target:
    types: [opened, reopened]
  issues:
    types: [opened, reopened]

permissions:
  pull-requests: write
  contents: read

jobs:
  agentscan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Cache AgentScan analysis
        uses: actions/cache@v4
        with:
          path: .agentscan-cache
          key: agentscan-cache-${{ github.actor }}
          restore-keys: agentscan-cache-
      - name: AgentScan
        uses: MatteoGabriele/agentscan-action@v1.3.0
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          skip-members: "matteogabriele"
          cache-path: ".agentscan-cache"
